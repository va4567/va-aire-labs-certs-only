apiVersion: kagent.dev/v1alpha2
kind: ModelConfig
metadata:
  name: default-llm
  namespace: kagent
spec:
  provider: OpenAI
  model: gpt-4o-mini
  apiKeySecret: kagent-openai
  apiKeySecretKey: OPENAI_API_KEY